# Runtime dependencies for the VeraSight ML ensemble.
# Train on Colab (latest torch) or locally; torch pin below matches the
# last version with official macOS x86_64 wheels (CPU-only).
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
scipy>=1.11
tqdm>=4.66
# On macOS x86_64, 2.2.2 is the last supported torch version (CPU-only).
# On Colab/Linux GPU, install the latest torch instead.
torch==2.2.2
# Convenience for downloading the public Express4D corpus (optional).
gdown>=5.0
